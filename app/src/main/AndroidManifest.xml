<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.test.testqskinloader"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".SkinLoaderApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".CurrentActivity"/>
        <activity android:name=".InstalledActivity"/>
        <activity android:name=".ChangeTextSizeActivity">
        </activity>
    </application>

</manifest>